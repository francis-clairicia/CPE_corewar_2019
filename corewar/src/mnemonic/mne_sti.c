/*
** EPITECH PROJECT, 2020
** mnemonic sti
** File description:
** corewar
*/

#include "corewar.h"

int mne_sti(champ_t *champ, battle_t *battle)
{
    //int to_adress = 0;
    //int fst_param = get_value(champ, battle);
    //int scd_param = get_value(champ, battle);
    //int thd_param = get_value(champ, battle);

    //if (is_register(fst_param) == 84)
    //    return 84;
    //if (is_register(scd_param) == 1)
    //    scd_param = champ->reg[scd_param - 1];
    //if (is_register(thd_param) == 1)
    //    thd_param = champ->reg[thd_param - 1];
    //to_adress = champ->pc + (scd_param % IDX_MOD) + (thd_param % IDX_MOD);
    //write_in_memory(champ->reg[fst_param - 1], to_adress, battle); // write in memory(content to write, to adress, battle with memory);
    return 0;
}